import 'package:flutter/material.dart';

class PageBlank extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: Center(
        child: Text('Esta página está en construcción'),
      ),
    );
  }
}